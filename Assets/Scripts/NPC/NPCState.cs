namespace Kingdoms.NPC
{
    /// <summary>
    /// Available states for NPCs
    /// </summary>
    public enum NPCState
    {
        Idle,       // Standing still, looking around
        Wander      // Walking randomly
    }
}
